# Dependency Management

- Use `pnpm` as the package manager for all projects.
- Define shared dependencies in the root `package.json` and project-specific dependencies in each package’s `package.json`.
- Run `pnpm install` from the root to ensure all workspaces are correctly linked.

