import { ScopedTranslatePipe } from './scoped-translate.pipe';

describe('ScopedTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new ScopedTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
