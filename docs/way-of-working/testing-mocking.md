# Testing & Mocking

- Use Jest for unit and integration testing.
- Mock external dependencies and Azure bindings (e.g., InvocationContext) in tests.
- Place tests alongside source files or in a dedicated `__tests__` directory.

